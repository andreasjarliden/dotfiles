" Source work specific vimrc file
let hostfile=expand('~/dotfiles/vimrc-work')
if filereadable(hostfile)
	execute 'source ' . hostfile
endif

let mapleader = ';'

set grepprg=ack

map <C-a> :pyf /opt/llvm/bin/clang-format.py<CR>
