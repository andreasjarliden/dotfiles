Contains .dotfiles which are hidden by default. 

Expected to be checked out to ~/dotfiles.  Run ./createSymlinks.sh from dotfiles directory to create symlinks for all the dotfiles, e.g. ~/.foo -> dotfiles/.foo
